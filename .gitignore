/target
/dist
/node_modules
/src-tauri/gen
/src-tauri/target
/src-tauri/binaries/
.DS_Store

# JetBrains project files. Removed from tracking in #114; ignored so they
# cannot be committed by accident again.
.idea/

# Impeccable live-overlay working state (transient; snapshots in critique/ are tracked)
.impeccable/live/

# Playwright e2e output (traces, screenshots, HTML report).
/test-results/
/playwright-report/
/blob-report/

# TypeScript incremental build stamp (tsc -b). Machine-local, never reviewable.
/*.tsbuildinfo

# Planning notes for future sessions. Working documents, not part of the app.
feature-plans/
