/target
/dist
/node_modules
/src-tauri/gen
/src-tauri/target
/src-tauri/binaries/
.DS_Store

# JetBrains project files. Removed from tracking in #114; ignored so they
# cannot be committed by accident again.
.idea/

# Impeccable working state (live overlay, local config, hook cache) plus the
# critique/audit snapshots, which were internal working notes and left the
# repo with the rest of the internal docs. design.json is the design-system
# definition and stays tracked.
.impeccable/*
!.impeccable/design.json

# Playwright e2e output (traces, screenshots, HTML report).
/test-results/
/playwright-report/
/blob-report/
